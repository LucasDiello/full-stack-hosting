{
    "version": 2,
    "name": "deploy-vercel-api",
    "builds": [
        {
            "src": "app.js",
            "use": "@vercel/node"
        }
    ],  
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/app.js"
        }
    ]
}